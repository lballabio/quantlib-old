#define max(a,b) (((a)>(b)) ? (a) : (b) )
#define ind(a,b) (((a)>(b)) ? (1) : (0) )

#include <stdlib.h>
#include <math.h>
#include <stdio.h>
#include "random.h"
#include "path_lrm_gam.h"

void main()
{   long M=5000;
    spgam(M);
} 
    
   
