DLLEXPORT LPXLOPER %s(
%s%s) {
    try {
%s%s%s        Properties properties = %s
%s%s);
        static XLOPER xRet;
        setArray(&xRet, properties, handle);
        return &xRet;
    } catch (const exception &e) {
        OH_LOG_MESSAGE(std::string("ERROR: %s: ") + e.what(), 2);
        return 0;
    }
}

