    try {
        ObjHandler::Properties properties = %s(
%s%s);
        propertiesToVaries(properties, result);
        return SUCCESS;
    } catch (const std::exception &e) {
        QuantLibAddin::QL_LOGMESSAGE("%s Error: "
            + std::string(e.what()));
        result = 0;
        return FAIL;
    }
}

