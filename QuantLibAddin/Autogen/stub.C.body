    try {
%s%s        Properties properties = 
            %s%s%s);
        propertiesToVaries(properties, result);
        return SUCCESS;
    } catch (const std::exception &e) {
        OH_LOG_MESSAGE("%s Error: "
            + std::string(e.what()));
        result = 0;
        return FAIL;
    }
}

