    try {
%s        ObjHandler::obj_ptr objectPointer(new QuantLibAddin::%s(%s));
        ObjHandler::storeObject(OUStringToStlString(handle), objectPointer);
        return handle;
    } catch (const std::exception &e) {
        ObjHandler::logMessage(std::string("ERROR: %s: ") + e.what(), 2);
        THROW_RTE;
    }
}

