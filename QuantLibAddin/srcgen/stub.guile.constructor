    try {
        std::string handle = GetChop<std::string>::scalar(x);
%s        ObjHandler::obj_ptr objectPointer(new QuantLibAddin::%s(%s));
        ObjHandler::storeObject(handle, objectPointer);
        return gh_str02scm(handle.c_str());
    } catch (const std::exception &e) {
        ObjHandler::logMessage(std::string("%s Error: ") + e.what(), 2);
        return SCM_EOL;
    }
}

